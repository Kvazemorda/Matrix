package setData;

import java.util.ArrayList;

public class TeamAndPeriod {
    ArrayList<String> listOfTeam;
    ArrayList<String> listOfPeriod;

    public ArrayList<String> getListOfPeriod() {
        listOfPeriod = new ArrayList<>();
        listOfPeriod.add("2017-2018");
        listOfPeriod.add("2016-2017");
        listOfPeriod.add("2015-2016");
        listOfPeriod.add("2014-2015");
        listOfPeriod.add("2013-2014");
        listOfPeriod.add("2012-2013");
        listOfPeriod.add("2011-2012");
        listOfPeriod.add("2010-2011");
        listOfPeriod.add("2009-2010");
        listOfPeriod.add("2008-2009");
        listOfPeriod.add("2007-2008");
        
        return listOfPeriod;
    }

    public ArrayList<String> getListOfTeam() {
        listOfTeam = new ArrayList<>();
        listOfTeam.add("TOR".toUpperCase());
        listOfTeam.add("STL".toUpperCase());
        listOfTeam.add("CGY".toUpperCase());
        listOfTeam.add("LAK".toUpperCase());
        listOfTeam.add("MTL".toUpperCase());
        listOfTeam.add("NYI".toUpperCase());
        listOfTeam.add("BOS".toUpperCase());
        listOfTeam.add("DET".toUpperCase());
        listOfTeam.add("NJD".toUpperCase());
        listOfTeam.add("WSH".toUpperCase());
        listOfTeam.add("MIN".toUpperCase());
        listOfTeam.add("CAR".toUpperCase());
        listOfTeam.add("ANA".toUpperCase());
        listOfTeam.add("CHI".toUpperCase());
        listOfTeam.add("EDM".toUpperCase());
        listOfTeam.add("PHI".toUpperCase());
        listOfTeam.add("SJS".toUpperCase());
        listOfTeam.add("WPJ".toUpperCase());
        listOfTeam.add("NYR".toUpperCase());
        listOfTeam.add("NSH".toUpperCase());
        listOfTeam.add("DAL".toUpperCase());
        listOfTeam.add("BUF".toUpperCase());
        listOfTeam.add("COL".toUpperCase());
        listOfTeam.add("OTT".toUpperCase());
        listOfTeam.add("PIT".toUpperCase());
        listOfTeam.add("ARI".toUpperCase());
        listOfTeam.add("FLO".toUpperCase());
        listOfTeam.add("TBL".toUpperCase());
        listOfTeam.add("CBJ".toUpperCase());
        listOfTeam.add("VAN".toUpperCase());
        listOfTeam.add("VGK".toUpperCase());

        return listOfTeam;
    }

}
